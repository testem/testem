{
  "name": "tape_example",
  "version": "0.0.0",
  "description": "Example tests using tape",
  "devDependencies": {
    "tape": "^4.0.0",
    "browserify": "^2.22.0"
  },
  "scripts": {
    "test": "node tests.js",
    "browserify": "browserify tests.js -o public/bundle.js"
  }
}
