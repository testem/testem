## Setup

This example stays on jasmine-core 5 so the split-boot path (`boot0.js`/`boot1.js`) stays covered. New projects should install `jasmine-core@^7`.

First install dependencies (`jasmine-core`)

    npm install

Then, just run tests

    npm test
