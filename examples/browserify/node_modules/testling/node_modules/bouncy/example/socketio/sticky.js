var bouncy = require('bouncy');
var sticky = {};
bouncy(function (req, bounce) {
    if (sticky[req.socket.remoteAddress]) {
        
    }
    else if (req.url.match(/^socket\.io/)) {
        bounce(5001);
    }
    else {
    }
}).listen(8081);
