## Setup

This example stays on jasmine-core 5 because `test.html` hardcodes `boot0.js`/`boot1.js`. jasmine-core 7 custom pages load `boot.js` only.

First install dependencies (`jasmine-core`)

    npm install

Then, just run tests

    npm test
